# ==================================================
# CONTOH KONFIGURASI .ENV UNTUK WINDOWS (PRODUCTION)
# ==================================================
# Copy file ini menjadi .env dan sesuaikan dengan environment Windows Anda

# ============
# APPLICATION
# ============
APP_NAME="Inventaris Barbershop"
APP_ENV=production
APP_KEY=base64:xxx  # Generate dengan: php artisan key:generate
APP_DEBUG=false  # PENTING: Set false untuk production
APP_URL=http://localhost/inventaris-barbershop/public

# =======
# LOGGING
# =======
LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error  # Hanya log error di production

# ========
# DATABASE
# ========
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=inventaris_barbershop
DB_USERNAME=root
DB_PASSWORD=  # Default XAMPP password kosong, ubah jika sudah di-set

# ========
# DRIVERS
# ========
BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

# ====
# MAIL (Optional - tidak diperlukan untuk offline)
# ====
MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# =====================================================
# CATATAN PENTING UNTUK WINDOWS:
# =====================================================
# 1. Setelah copy file ini jadi .env, jalankan:
#    php artisan key:generate
#
# 2. Jika ingin menggunakan domain custom (misal: inventaris.local):
#    - Edit C:\Windows\System32\drivers\etc\hosts (as Administrator)
#      Tambahkan: 127.0.0.1 inventaris.local
#    - Update APP_URL=http://inventaris.local
#
# 3. Jika ingin diakses dari jaringan lokal (LAN):
#    - Cek IP Windows dengan: ipconfig
#    - Update APP_URL=http://192.168.1.xxx (IP Windows Anda)
#    - Setting Windows Firewall untuk allow port 80/8000
#
# 4. Untuk security lebih baik, ubah default password MySQL:
#    DB_USERNAME=inventaris_user
#    DB_PASSWORD=password_kuat_anda
#
# 5. Jika menggunakan PHP built-in server (php artisan serve):
#    APP_URL=http://localhost:8000
# =====================================================
